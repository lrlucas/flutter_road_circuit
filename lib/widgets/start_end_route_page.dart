import 'package:flutter/material.dart';


class StartEndRoutePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      body: Container(
        child: Center(
          child: Text('StarEndRoutePage'),
        ),
      ),
    );
  }
}